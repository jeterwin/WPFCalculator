<UserControl x:Class="Calculator.UserControls.CustomButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Calculator.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="75" d:DesignWidth="75">
    <Grid>
        <Button Background="{Binding Color}" BorderThickness="0" Click="Button_Click">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"></Setter>
                </Style>
            </Button.Resources>
            <TextBlock Name="btnName" FontSize="24" UseLayoutRounding="True" Text="{Binding Number}" Foreground="White" VerticalAlignment="Center"
                       HorizontalAlignment="Center" FontFamily="Courier New"></TextBlock>
        </Button>
    </Grid>
</UserControl>
